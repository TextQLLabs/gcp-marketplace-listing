{{- if eq (include "tql.secretsMode" .) "externalSecrets" }}
{{- $p := include "tql.externalSecretsPrefix" . }}
{{ include "tql.externalSecretHeader" (dict "name" "tableau-internal-secret" "root" .) }}
    - secretKey: secret
      remoteRef:
        key: {{ $p }}-tableau-internal-secret
{{- end }}
