{{- if eq (include "tql.secretsMode" .) "externalSecrets" }}
{{- $p := include "tql.externalSecretsPrefix" . }}
{{ include "tql.externalSecretHeader" (dict "name" "deployment-secrets" "root" .) }}
    - secretKey: deployment.private.key
      remoteRef:
        key: {{ $p }}-deployment-private-key
{{- end }}
