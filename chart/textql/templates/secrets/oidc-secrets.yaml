{{- if eq (include "tql.secretsMode" .) "externalSecrets" }}
{{- $p := include "tql.externalSecretsPrefix" . }}
{{ include "tql.externalSecretHeader" (dict "name" "oidc-secrets" "root" .) }}
    - secretKey: client.secret
      remoteRef:
        key: {{ $p }}-oidc-client-secret
{{- end }}
