{{- if .Values.oathkeeper.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-oathkeeper
  labels:
    component: auth
    {{- include "tql.labels" . | nindent 4 }}
{{- end }}
